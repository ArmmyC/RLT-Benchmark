schema_version: "rfid_apbench_activity/v0.1"
workload_id: ap_009_serial_parity_accumulator_default
clock: clk
reset: rst_n
measurement_window:
  start_time: 0
  end_time: 200
stimulus:
  type: deterministic
  description: "Reset, four serial bits, invalid-cycle hold, and synchronous clear."
vcd:
  include_scope: testbench
  exclude_signals:
    - clk
activity_metric: total_signal_toggles
