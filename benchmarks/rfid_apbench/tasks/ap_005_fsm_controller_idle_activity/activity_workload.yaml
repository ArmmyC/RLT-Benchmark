schema_version: "rfid_apbench_activity/v0.1"
workload_id: ap_005_fsm_controller_idle_activity_default
clock: clk
reset: rst_n
measurement_window:
  start_time: 0
  end_time: 260
stimulus:
  type: deterministic
  description: "Reset, extended idle, valid response path, return idle, and error path."
vcd:
  include_scope: testbench
  exclude_signals:
    - clk
activity_metric: total_signal_toggles
