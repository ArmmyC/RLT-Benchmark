# v0.7 RFID-APBench Targeted Empty-Response Observability Rerun

## Scope

This report records a bounded targeted rerun attempt for the two public/synthetic RFID-APBench tasks with concentrated historical empty responses:

- `ap_001_idle_counter`
- `ap_010_retry_timeout_fsm`

Exactly three sample slots were recorded for each task, for six sanitized rows total. The required model-listing preflight failed before generation, so no model request was attempted and no empty or non-empty completion was observed.

## Run Configuration

- Run id: `20260617T203640Z`
- Model: `qwen36-27b`
- Prompt profile: `neutral_baseline`
- Temperature: `0.0`
- Top-p: `1.0`
- Max tokens: `4096`
- Targeted tasks: 2
- Samples per task: 3
- Targeted sample rows: 6
- Actual generation requests attempted: 0
- Endpoint status: `unavailable`
- Endpoint: `http://127.0.0.1:8000/v1`
- Icarus Verilog compile: `healthy`
- Icarus runtime `vvp`: `healthy`
- Yosys synthesis: `healthy`

The endpoint location is reported without credentials or secrets.

## Non-Goals

- No benchmark task, prompt, prompt profile, reference, testbench, workload, metric, extractor, evaluator, or scoring formula is changed.
- No benchmark-wide baseline, prompt tuning, model comparison, private evaluation, training-data creation, fine-tuning, or adapter work is performed.
- No raw prompt, response text, response body, generated RTL, simulator or synthesis log, VCD, compiled artifact, tool scratch, secret, model weight, or dataset is committed.

## Endpoint Blocker

The configured endpoint did not answer the required model-listing preflight. The runner therefore emitted six explicit blocker rows rather than faking generation or classifying absent output as an empty model response.

| request outcome | rows |
| --- | ---: |
| `endpoint_unavailable` | 6 |

All rows have `request_attempt_count=0` and `response_parse_status=not_attempted`. This is an endpoint blocker, not `success_empty`, timeout, HTTP error, transport error, response parse failure, or response schema failure.

## Aggregate Gate Counts

| gate or metric | count | denominator |
| --- | ---: | ---: |
| targeted sample rows | 6 | 6 |
| generation request attempted | 0 | 6 |
| generation completed | 0 | 6 |
| extraction passed | 0 | 6 |
| candidate available | 0 | 6 |
| compile passed | 0 | 6 |
| correctness passed | 0 | 6 |
| synthesis passed | 0 | 6 |
| valid score | 0 | 6 |

No downstream gate ran because no candidate was generated. Zero gate counts are blocker outcomes and must not be interpreted as candidate failures.

## Metadata Availability

| sanitized field | available rows | observed value or reason |
| --- | ---: | --- |
| `request_outcome` | 6/6 | `endpoint_unavailable` |
| `request_attempt_count` | 6/6 | `0` |
| `latency_seconds` | 0/6 | No generation request attempted. |
| `response_choice_count` | 0/6 | No response received. |
| `response_content_present` | 0/6 | No response received. |
| `response_character_count` | 0/6 | No response received. |
| `finish_reason` | 0/6 | Endpoint-dependent response field unavailable. |
| `prompt_tokens` | 0/6 | No generation usage returned. |
| `completion_tokens` | 0/6 | No generation usage returned. |
| `total_tokens` | 0/6 | No generation usage returned. |
| `http_status_class` | 0/6 | Required preflight did not produce a generation response status. |
| `response_parse_status` | 6/6 | `not_attempted` |

## Per-Sample Sanitized Summary

| task id | sample | request outcome | attempts | content present | characters | finish reason | prompt tokens | completion tokens | total tokens | HTTP class | parse status |
| --- | ---: | --- | ---: | --- | ---: | --- | ---: | ---: | ---: | --- | --- |
| `ap_001_idle_counter` | 1 | `endpoint_unavailable` | 0 | unavailable |  | unavailable |  |  |  | unavailable | `not_attempted` |
| `ap_001_idle_counter` | 2 | `endpoint_unavailable` | 0 | unavailable |  | unavailable |  |  |  | unavailable | `not_attempted` |
| `ap_001_idle_counter` | 3 | `endpoint_unavailable` | 0 | unavailable |  | unavailable |  |  |  | unavailable | `not_attempted` |
| `ap_010_retry_timeout_fsm` | 1 | `endpoint_unavailable` | 0 | unavailable |  | unavailable |  |  |  | unavailable | `not_attempted` |
| `ap_010_retry_timeout_fsm` | 2 | `endpoint_unavailable` | 0 | unavailable |  | unavailable |  |  |  | unavailable | `not_attempted` |
| `ap_010_retry_timeout_fsm` | 3 | `endpoint_unavailable` | 0 | unavailable |  | unavailable |  |  |  | unavailable | `not_attempted` |

Empty cells correspond to JSON `null` and empty CSV values in the stable observability schema. They do not represent zero tokens, zero characters, or false content presence.

## Empty-Response Interpretation

The rerun provides no new empty or non-empty completion evidence. None of the six rows is `success_empty`, and no response metadata exists to apply the zero-token, null-content, whitespace-only, parse-failure, or schema-failure rules.

The only supported interpretation is that the endpoint was unavailable at required preflight time. Treating these blocker rows as repeated empty responses would be incorrect.

## Comparison With Previous Evidence

| task id | previous sanitized evidence | this rerun attempt |
| --- | --- | --- |
| `ap_001_idle_counter` | 3/3 empty in the expanded baseline; 5/6 empty across the expanded and earlier post-tool-health baselines. | Three blocker rows, zero generation requests, no completion evidence. |
| `ap_010_retry_timeout_fsm` | 3/3 empty in the expanded baseline. | Three blocker rows, zero generation requests, no completion evidence. |

Historical empty-response evidence remains unchanged and cannot be backfilled with the new metadata. This blocked attempt neither confirms nor contradicts the prior task concentration.

## Benchmark Asset Decision

Keep benchmark assets unchanged. The endpoint blocker occurred before prompt submission, extraction, evaluation, or scoring and provides no evidence against either task, prompt, reference, testbench, workload, extractor, evaluator, metric, or scoring rule.

## Fine-Tuning Readiness

Fine-tuning is out of scope and is not recommended. An endpoint-blocked rerun adds no model-output evidence, training dataset, causal diagnosis, or governance basis for training or adapter work.

## Recommended Next Step

Recommend exactly one next step: `no_immediate_followup_beyond_documentation`.

Under the specified decision rules, the blocked rerun adds no useful completion evidence: no timeout, HTTP error, transport error, retry instability, parsed empty response, or schema/client-normalization issue was observed on a generation request. This report preserves the blocker honestly without proposing prompt changes or another immediate experiment.

## Activity Proxy Caveat

Activity is a VCD toggle-count proxy from the declared public workload. It is not measured silicon power, signoff power, final silicon PPA, or production QoR.

## Public Synthetic Caveat

This rerun record covers only public/synthetic RFID-APBench tasks. It contains no private data or private RTL, is not private evaluation, and is not fine-tuning or training-readiness evidence.
