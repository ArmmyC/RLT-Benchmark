# Feature Spec: v0.7 RFID-APBench Thinking-Disabled Closeout

## Goal

Close the v0.7 RFID-APBench thinking-disabled investigation thread and preserve the evidence that forwarding `chat_template_kwargs.enable_thinking=false` resolved the targeted null-content token-limit symptom within the bounded public/synthetic validation evidence.

The targeted 3-sample thinking-disabled validation recommends `v0.7_thinking_disabled_closeout_spec` because no null-content token-limit row recurred after the RFID runner began forwarding the configured Qwen thinking-disable extra body.

This closeout is documentation and planning only. It is not another model run, benchmark-wide baseline, prompt-profile experiment, model comparison, private evaluation, fine-tuning, or training-data creation.

## Inputs

Read first:

- `reports/v0.7_rfid_apbench_targeted_3sample_thinking_disabled_validation.md`
- `reports/v0.7_rfid_apbench_targeted_3sample_thinking_disabled_validation.csv`
- `reports/v0.7_rfid_apbench_targeted_3sample_thinking_disabled_validation.jsonl`
- `reports/v0.7_rfid_apbench_bounded_thinking_disabled_validation.md`
- `reports/v0.7_rfid_apbench_thinking_mode_configuration_bugfix.md`
- `reports/v0.7_rfid_apbench_endpoint_runtime_investigation.md`
- `reports/v0.7_rfid_apbench_profile_rejected_closeout.md`
- `reports/v0.7_rfid_apbench_bounded_prompt_profile_experiment.md`
- `reports/v0.7_rfid_apbench_targeted_empty_response_observability_rerun.md`
- `configs/models.yaml`
- `scripts/run_rfid_apbench_3sample_baseline.py`

## Required Scope

Create a sanitized closeout report that:

1. Summarizes the original empty/null-content token-limit symptom.
2. Summarizes the endpoint-runtime root cause investigation.
3. Summarizes the thinking-mode propagation bugfix.
4. Summarizes the one-sample and targeted 3-sample post-fix validations.
5. States that the null-content token-limit symptom is resolved within the targeted validation evidence.
6. Separates output-completion reliability from functional correctness.
7. Notes the persistent `ap_001_idle_counter` simulation failures as a correctness issue, not an empty-response issue.
8. Notes the `ap_010_retry_timeout_fsm` targeted post-fix samples passed functional evaluation and scoring.
9. States that `completion_reliability` remains rejected and unused.
10. States that raising global max tokens is not justified by this evidence.
11. States benchmark assets remain unchanged.
12. States fine-tuning remains out of scope and not recommended.
13. Recommends exactly one next bounded direction.

## Required Files

Create:

- `reports/v0.7_rfid_apbench_thinking_disabled_closeout.md`

Optionally update:

- `docs/specs/v0.7-rfid-apbench-thinking-disabled-closeout.md` with implementation status after the report exists

Do not run another model evaluation in this spec.

## Report Requirements

The Markdown report must include:

- scope and non-goals
- source reports used
- timeline of the empty-response investigation
- pre-fix evidence summary
- bugfix summary
- post-fix one-sample evidence summary
- post-fix targeted 3-sample evidence summary
- response-boundary conclusion
- correctness/evaluation caveat, especially `ap_001_idle_counter`
- token-limit decision
- prompt-profile decision
- benchmark asset decision
- fine-tuning readiness decision
- exactly one recommended next bounded direction
- activity proxy caveat
- public synthetic / no private / not fine-tuning caveat

## Decision Rules

Recommend exactly one:

- `post_fix_expanded_10task_baseline_spec` if the closeout concludes the targeted response-boundary issue is resolved enough to rerun the current 10-task RFID-APBench baseline with the fixed runner.
- `ap001_correctness_investigation_spec` if the closeout concludes the persistent `ap_001_idle_counter` simulation failures should be investigated before broader reruns.
- `v0.7_release_tag_spec` if no more validation or analysis is needed before tagging v0.7.
- `no_immediate_followup_beyond_documentation` if no further work is justified now.

Do not recommend max-token increase, additional prompt-profile experiments, model comparison, private evaluation, training-data creation, or fine-tuning from this closeout.

## Tests

Run:

```bash
python -m pytest
```

Add tests only if helper code is introduced. Do not require a live model endpoint in unit tests.

## Forbidden

Do not commit:

- private SiliconCraft RTL
- private task text
- raw prompts beyond public benchmark prompts/profile text
- raw model responses
- response bodies
- generated model RTL
- model outputs outside sanitized reports
- raw benchmark output directories
- VCD files
- simulator logs
- synthesis logs
- compiled simulation artifacts
- tool scratch files
- secrets or endpoint credentials
- model weights
- training datasets
- LoRA, QLoRA, or DoRA adapters
- fine-tuning scripts

## Non-Goals

Do not:

- add or modify benchmark tasks
- edit benchmark task prompt files
- run a benchmark-wide model evaluation
- compare different models
- change prompt profiles
- raise the global max-token default
- create training data
- fine-tune a model
- add private benchmarks
- claim measured power
- claim final silicon PPA
- change area/activity scoring formula
- change evaluator policy

## Definition of Done

Done when:

1. `reports/v0.7_rfid_apbench_thinking_disabled_closeout.md` exists.
2. It states whether the null-content token-limit symptom is resolved within targeted validation evidence.
3. It separates response reliability from functional correctness.
4. It preserves the `ap_001_idle_counter` correctness caveat.
5. It states benchmark assets remain unchanged.
6. It states fine-tuning is not recommended from this evidence alone.
7. It recommends exactly one next bounded direction.
8. `python -m pytest` passes.
9. No raw/private/model-output/training artifacts are committed.
